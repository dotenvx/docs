<CodeGroup>

```bash {{ title: "cURL" }}
curl -sfS https://dotenvx.sh/vlt | sh
```

```bash {{ title: "npm" }}
npm install -g @dotenvx/dotenvx-vlt
```

```bash {{ title: "Windows" }}
winget install dotenvx-vlt
```

</CodeGroup>
