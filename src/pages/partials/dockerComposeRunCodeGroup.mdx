<CodeGroup>

```bash {{ title: "Node" }}
docker-compose up --build
```

```bash {{ title: "Python" }}
docker-compose up --build
```

```bash {{ title: "PHP" }}
docker-compose up --build
```

```bash {{ title: "Ruby" }}
docker-compose up --build
```

```bash {{ title: "Go" }}
docker-compose up --build
```

```bash {{ title: "Rust" }}
docker-compose up --build
```

</CodeGroup>
