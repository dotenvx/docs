<CodeGroup>

```bash {{ title: "Node" }}
CMD ["dotenvx", "run",  "--env-file=.env.production", "--", "node", "index.js"]
```

```bash {{ title: "Python" }}
CMD ["dotenvx", "run",  "--env-file=.env.production", "--", "gunicorn", "--workers", "4", "--bind", "0.0.0.0:5000", "wsgi:app"]
```

```bash {{ title: "PHP" }}
CMD ["dotenvx", "run", "--env-file=.env.production", "--", "apache2-foreground"]
```

```bash {{ title: "Ruby" }}
CMD ["dotenvx", "run", "--env-file=.env.production", "--", "ruby", "index.rb", "-o", "0.0.0.0"]
```

```bash {{ title: "Go" }}
CMD ["dotenvx", "run", "--env-file=.env.production", "--", "./main"]
```

```bash {{ title: "Rust" }}
```

</CodeGroup>
