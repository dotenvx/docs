<CodeGroup>

```bash {{ title: "Node" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "Python" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "PHP" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "Ruby" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "Go" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "Rust" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

</CodeGroup>
