<CodeGroup>

```bash {{ title: "Node" }}
docker build -t app . && docker run -it --init --rm -p 3000:3000 app
```

```bash {{ title: "Python" }}
docker build -t app . && docker run -it --init --rm -p 5000:5000 app
```

```bash {{ title: "PHP" }}
docker build -t app . && docker run -it --init --rm -p 8080:80 app
```

```bash {{ title: "Ruby" }}
docker build -t app . && docker run -it --init --rm -p 4567:4567 app
```

```bash {{ title: "Go" }}
```

```bash {{ title: "Rust" }}
```

</CodeGroup>
