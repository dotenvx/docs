<CodeGroup>

```bash {{ title: "Node" }}
git commit -am "initial commit"
heroku create
git push heroku
```

```bash {{ title: "Python" }}
git commit -am "initial commit"
heroku create
git push heroku
```

```bash {{ title: "PHP" }}
git commit -am "initial commit"
heroku create
git push heroku
```

```bash {{ title: "Ruby" }}
git commit -am "initial commit"
heroku create
git push heroku
```

```bash {{ title: "Go" }}
git commit -am "initial commit"
heroku create
git push heroku
```

```bash {{ title: "Rust" }}
echo "VERSION=nightly" > RustConfig
git commit -am "initial commit"
heroku create
heroku buildpacks:set emk/rust
git push heroku
```

</CodeGroup>
