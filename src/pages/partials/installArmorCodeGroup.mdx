<CodeGroup>

```bash {{ title: "cURL" }}
curl -sfS https://dotenvx.sh/install.sh | sh
```

```bash {{ title: "npm" }}
npm install -g @dotenvx/dotenvx
```

```bash {{ title: "Windows" }}
winget install dotenvx
```

</CodeGroup>
