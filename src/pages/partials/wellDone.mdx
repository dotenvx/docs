<Note>
  You succesfully used encrypted envsd to deploy your secrets. This is much safer than scattering your secrets across multiple third-party platforms and tools. Whenever you need to add or change a secret, just re-encrypt with `dotenvx set KEY value --encrypt` and redeploy.
</Note>
