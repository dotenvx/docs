import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Print hostname.'

<Breadcrumb
  items={[
    { label: 'CLI Reference', href: '/docs/cli' },
    { label: 'Vault ⛨', href: '/docs/cli/vlt' },
    { label: 'Settings', href: '/docs/cli/vlt/settings' },
    { label: 'hostname', href: '/docs/cli/vlt/settings-hostname' },
  ]}
/>

# Vault ⛨ Settings hostname

<GuideHeading lead={description} />

```sh
$ dotenvx-vlt settings hostname
https://vlt.dotenvx.com
```

Note: By default this is `vlt.dotenvx.com`. This setting exists to support future self-hosted instances of Dotenvx Vlt. [Get in touch](mailto:mot@dotenvx.com) if your company needs this.
