import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Push armored key from .env.keys.'

<Breadcrumb
  items={[
    { label: 'CLI Reference', href: '/docs/cli' },
    { label: 'VLT ⛨', href: '/docs/cli/vlt' },
    { label: 'Armor', href: '/docs/cli/vlt/armor' },
    { label: 'push', href: '/docs/cli/vlt/armor-push' },
  ]}
/>

# VLT ⛨ Armor push

<GuideHeading lead={description} />

```sh
$ dotenvx-vlt armor push
```

Use a specific env file.

```sh
$ dotenvx-vlt armor push -f .env.production
```

Use a token or team.

```sh
$ dotenvx-vlt armor push --token token --team team
```
