import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Set hostname for login.'

<Breadcrumb
  items={[
    { label: 'CLI Reference', href: '/docs/cli' },
    { label: 'Armor', href: '/docs/cli/armor' },
    { label: 'Login', href: '/docs/cli/armor/login' },
    { label: 'hostname', href: '/docs/cli/armor/login-hostname' },
  ]}
/>

# Armor Login --hostname

<GuideHeading lead={description} />

```sh
$ dotenvx armor login --hostname https://armor.dotenvx.com
```
