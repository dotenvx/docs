import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Print hostname.'

<Breadcrumb
  items={[
    { label: 'CLI Reference', href: '/docs/cli' },
    { label: 'Armor', href: '/docs/cli/armor' },
    { label: 'Settings', href: '/docs/cli/armor/settings' },
    { label: 'hostname', href: '/docs/cli/armor/settings-hostname' },
  ]}
/>

# Armor Settings hostname

<GuideHeading lead={description} />

```sh
$ dotenvx armor settings hostname
https://armor.dotenvx.com
```

Note: By default this is `armor.dotenvx.com`. This setting exists to support future self-hosted instances of Dotenvx Armor. [Get in touch](mailto:mot@dotenvx.com) if your company needs this.
