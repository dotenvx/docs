import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description = 'Move private keys into your OS secret store.'

<Breadcrumb
  items={[
    { label: 'CLI Reference', href: '/docs/cli' },
    { label: 'Native', href: '/docs/cli/native' },
  ]}
/>

# Native

<GuideHeading lead={description} />

Native uses your operating system's secret store:

- macOS: Keychain
- Windows: Credential Manager
- Linux: Secret Service through `secret-tool`

On Linux, `secret-tool` and an available Secret Service are required.

```sh
$ dotenvx native
```

## Commands

- [up](/docs/cli/native/up)
- [down](/docs/cli/native/down)
- [push](/docs/cli/native/push)
- [pull](/docs/cli/native/pull)
