import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'
export const description =
  'Generate a .env.example file inside the specified directory. Useful for monorepos.'

<Breadcrumb
  items={[
    { label: "CLI Reference", href: "/docs/cli" },
    { label: "Extensions", href: "/docs/cli/ext" },
  ]}
/>
# dotenvx ext genexample directory

<GuideHeading lead={description} />

```sh
$ echo "HELLO=World" > .env
mkdir -p apps/backend
echo "HELLO=Backend" > apps/backend/.env

dotenvx ext genexample apps/backend
▣ generated (.env.example)
```

```ini {{ title: "apps/backend/.env.example" }}
# apps/backend/.env.example
HELLO=""
```
