import InstallCodeGroup from '@/pages/partials/installCodeGroup.mdx'
import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Learn how to encrypt your first .env file using .NET and dotenvx.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: '.NET', href: '/docs/secrets-in-dotnet' },
  ]}
/>

# Encrypt secrets in .NET

<GuideHeading lead={description} />

## Prerequisites

To get the most out of this guide, you'll need to:

* [Create your account](https://dotenvx.com/signup) (optional)

## 1. Install

Get the Dotenvx CLI.

<InstallCodeGroup/>

## 2. Encrypt env file

Encrypt your `.env` file.

```sh
dotenvx encrypt
```

## 3. Inject secrets

Then inject your encrypted secrets at runtime with `dotenvx run`.

```csharp {{ title: 'Program.cs' }}
var hello = Environment.GetEnvironmentVariable("HELLO");
Console.WriteLine($"Hello {hello}");
```

```sh
dotenvx run -- dotnet run
```
