import InstallNodeSdkCodeGroup from '@/pages/partials/installNodeSdkCodeGroup.mdx'
import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'
import EncryptWithArmorNpxCodeGroup from '@/pages/partials/encryptWithArmorNpxCodeGroup.mdx'

export const description =
  'Encrypt a .env file in Expo with the Dotenvx Node.js SDK, commit it safely, and load its secrets at runtime.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: 'Node.js', href: '/docs/secrets-in-nodejs' },
    { label: 'Expo', href: '/docs/secrets-in-expo' },
  ]}
/>

# Encrypt a .env file in Expo

<GuideHeading lead={description} />

## 1. Install

Get the Dotenvx Node.js SDK.

<InstallNodeSdkCodeGroup/>

## 2. Encrypt

Encrypt your .env file.

<EncryptWithArmorNpxCodeGroup/>

## 3. Inject

Preface Expo scripts with `dotenvx run --`.

```json {{ title: 'package.json' }}
{
  "scripts": {
    "start": "dotenvx run -- expo start",
    "reset-project": "node ./scripts/reset-project.js",
    "android": "dotenvx run -- expo start --android",
    "ios": "dotenvx run -- expo start --ios",
    "web": "dotenvx run -- expo start --web",
    "lint": "expo lint"
  }
}
```
