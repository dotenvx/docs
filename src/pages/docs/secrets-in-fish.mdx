import InstallWithArmorCurlCodeGroup from '@/pages/partials/installWithArmorCurlCodeGroup.mdx'
import EncryptWithArmorCodeGroup from '@/pages/partials/encryptWithArmorCodeGroup.mdx'
import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Learn how to encrypt your first .env file using Fish and dotenvx.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: 'Other', href: '/docs/secrets-in-other' },
  ]}
/>

# Encrypt secrets in Fish

<GuideHeading lead={description} />

## Prerequisites

To get the most out of this guide, you'll need to:

* [Create your account](https://dotenvx.com/signup) (optional)

## 1. Install

Get the Dotenvx CLI.

<InstallWithArmorCurlCodeGroup/>

## 2. Encrypt

Encrypt your .env file.

<EncryptWithArmorCodeGroup/>

## 3. Inject

Then inject your encrypted secrets at runtime with `dotenvx run`.

```sh
dotenvx run --quiet -- sh -c 'echo Hello $HELLO'
```
