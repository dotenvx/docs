import InstallWithOpsCurlCodeGroup from '@/pages/partials/installWithOpsCurlCodeGroup.mdx'
import EncryptWithOpsCodeGroup from '@/pages/partials/encryptWithOpsCodeGroup.mdx'
import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Learn how to encrypt your first .env file using Rust and dotenvx.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: 'Rust', href: '/docs/secrets-in-rust' },
  ]}
/>

# Encrypt secrets in Rust

<GuideHeading lead={description} />

## Prerequisites

To get the most out of this guide, you'll need to:

* [Create your account](https://dotenvx.com/signup) (optional)

## 1. Install

Get the Dotenvx CLI.

<InstallWithOpsCurlCodeGroup/>

## 2. Encrypt env file

Encrypt your `.env` file.

<EncryptWithOpsCodeGroup/>

## 3. Inject

Then inject your encrypted secrets at runtime with `dotenvx run`.

```rust {{ title: 'src/main.rs' }}
use std::env;

fn main() {
    let hello = env::var("HELLO").unwrap_or_default();
    println!("HELLO: {}", hello);
}
```

```sh
dotenvx run -- cargo run
```
