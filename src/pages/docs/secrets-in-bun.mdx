import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'
import InstallBunSdkCodeGroup from '@/pages/partials/installBunSdkCodeGroup.mdx'
import EncryptWithArmorBunxCodeGroup from '@/pages/partials/encryptWithArmorBunxCodeGroup.mdx'

export const description =
  'Learn how to encrypt your first .env file using Bun and the Dotenvx Node.js SDK.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: 'Node.js', href: '/docs/secrets-in-nodejs' },
    { label: 'Bun', href: '/docs/secrets-in-bun' },
  ]}
/>

# Encrypt secrets in Bun

<GuideHeading lead={description} />

## 1. Install

Install the Dotenvx Node.js SDK with Bun.

<InstallBunSdkCodeGroup />

## 2. Encrypt

Encrypt your .env file.

<EncryptWithArmorBunxCodeGroup/>

## 3. Inject

Then inject your encrypted secrets at runtime.

```js {{ title: 'index.js' }}
require('@dotenvx/dotenvx').config()

console.log(`HELLO: ${process.env.HELLO}`)
```
