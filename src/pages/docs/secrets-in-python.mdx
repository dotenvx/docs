import InstallWithVltCurlCodeGroup from '@/pages/partials/installWithVltCurlCodeGroup.mdx'
import EncryptWithVltCodeGroup from '@/pages/partials/encryptWithVltCodeGroup.mdx'
import { Breadcrumb } from '@/components/Breadcrumb'
import { GuideHeading } from '@/components/GuideHeading'

export const description =
  'Learn how to encrypt your first .env file using Python and dotenvx.'

<Breadcrumb
  items={[
    { label: 'Quickstart', href: '/docs/quickstart' },
    { label: 'Python', href: '/docs/secrets-in-python' },
  ]}
/>

# Encrypt secrets in Python

<GuideHeading lead={description} />

## Prerequisites

To get the most out of this guide, you'll need to:

* [Create your account](https://dotenvx.com/signup) (optional)

## 1. Install

Get the Dotenvx CLI.

<InstallWithVltCurlCodeGroup/>

## 2. Encrypt

Encrypt your .env file.

<EncryptWithVltCodeGroup/>

## 3. Inject

Then inject your encrypted secrets at runtime with `dotenvx run`.

```python {{ title: 'main.py' }}
import os

print(f"HELLO: {os.getenv('HELLO')}")
```

```sh
dotenvx run -- python main.py
```
