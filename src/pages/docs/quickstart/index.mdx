import { Video } from '@/components/Video'
import { ScreenshotBrowser } from '@/components/ScreenshotBrowser'
import InstallCodeGroup from '@/pages/partials/installCodeGroup.mdx'
import InstallLibariesCodeGroup from '@/pages/partials/installLibrariesCodeGroup.mdx'
import HelloWorldCodeGroup from '@/pages/partials/helloWorldCodeGroup.mdx'

import { GoIcon } from '@/components/icons/GoIcon'
import { NodeIcon } from '@/components/icons/NodeIcon'
import { PhpIcon } from '@/components/icons/PhpIcon'
import { PythonIcon } from '@/components/icons/PythonIcon'
import { RubyIcon } from '@/components/icons/RubyIcon'
import { RustIcon } from '@/components/icons/RustIcon'

export const description =
  'Manage secrets with dotenvx'

##### [dotenvx](/docs)
# Quickstart ⚡️

Manage secrets with `dotenvx`. {{ className: 'lead' }} 

<div className="not-prose grid grid-flow-col auto-cols-max gap-x-6 gap-y-10">
  <NodeIcon className="h-9 w-9 text-[#339933]"/>
  <PythonIcon className="h-9 w-9 text-[#3776AB]"/>
  <RubyIcon className="h-9 w-9 text-[#CC342D]"/>
  <GoIcon className="h-10 w-10 text-[#00ADD8]"/>
  <PhpIcon className="h-10 w-10 text-[#777BB4]"/>
  <RustIcon className="h-10 w-10 text-[#000000] dark:invert"/>
</div>

## Setup

Install `dotenvx`.

<InstallCodeGroup />

Install the necessary web server libraries in the language of your choice.

<InstallLibariesCodeGroup/>

Create a simple *Hello World* program.

<HelloWorldCodeGroup/>

Run it.

<CodeGroup>

```bash {{ title: "Node" }}
$ node index.js
Server running on port:3000
# http://localhost:3000
# Hello undefined
```

```bash {{ title: "Python" }}
$ flask --app index run
Running on http://127.0.0.1:5000
# http://127.0.0.1:5000
# Hello None
```

```bash {{ title: "PHP" }}
$ php index.php
PHP Warning:  Undefined array key "HELLO"
# Hello
```

```bash {{ title: "Ruby" }}
$ ruby index.rb
Listening on http://127.0.0.1:4567
# http://127.0.0.1:4567
# Hello
```

```bash {{ title: "Go" }}
$ go run main.go
http server started on [::]:1323
# http://localhost:1323
# Hello
```

```bash {{ title: "Rust" }}
$ cargo run
Rocket has launched from http://127.0.0.1:8000
# http://127.0.0.1:8000
# Hello
```


</CodeGroup>

## Run Command

Run it with `dotenvx run --`.

<CodeGroup>

```bash {{ title: "Node" }}
$ dotenvx run -- node index.js
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
Server running on port:3000
# http://localhost:3000
# Hello undefined
```

```bash {{ title: "Python" }}
$ dotenvx run -- flask --app index run
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
Running on http://127.0.0.1:5000
# http://127.0.0.1:5000
# Hello None
```

```bash {{ title: "PHP" }}
$ dotenvx run -- php index.php
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
PHP Warning:  Undefined array key "HELLO"
# Hello
```

```bash {{ title: "Ruby" }}
$ dotenvx run -- ruby index.rb
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
Listening on http://127.0.0.1:4567
# http://127.0.0.1:4567
# Hello
```

```bash {{ title: "Go" }}
$ dotenvx run -- go run main.go
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
http server started on [::]:1323
# http://localhost:1323
# Hello
```

```bash {{ title: "Rust" }}
$ dotenvx run -- cargo run
[dotenvx@0.38.0] missing .env file (.env)
[dotenvx@0.38.0] injecting env (0)
Rocket has launched from http://127.0.0.1:8000
# http://127.0.0.1:8000
# Hello
```

</CodeGroup>

`dotenvx` is more helpful than [dotenv](https://github.com/motdotla/dotenv). It warns you of a missing `.env` file.

Create the `.env` file.

```ini
# .env
JELLO="World"
```

Run it again.

<CodeGroup>

```bash {{ title: "Node" }}
$ dotenvx run -- node index.js
[dotenvx@0.38.0] injecting env (1) from .env
Server running on port:3000
# http://localhost:3000
# Hello undefined
```

```bash {{ title: "Python" }}
$ dotenvx run -- flask --app index run
[dotenvx@0.38.0] injecting env (1) from .env
Running on http://127.0.0.1:5000
# http://127.0.0.1:5000
# Hello None
```

```bash {{ title: "PHP" }}
$ dotenvx run -- php index.php
[dotenvx@0.38.0] injecting env (1) from .env
PHP Warning:  Undefined array key "HELLO"
# Hello
```

```bash {{ title: "Ruby" }}
$ dotenvx run -- ruby index.rb
[dotenvx@0.38.0] injecting env (1) from .env
Listening on http://127.0.0.1:4567
# http://127.0.0.1:4567
# Hello
```

```bash {{ title: "Go" }}
$ dotenvx run -- go run main.go
[dotenvx@0.38.0] injecting env (1) from .env
http server started on [::]:1323
# http://localhost:1323
# Hello
```

```bash {{ title: "Rust" }}
$ dotenvx run -- cargo run
[dotenvx@0.38.0] injecting env (1) from .env
Rocket has launched from http://127.0.0.1:8000
# http://127.0.0.1:8000
# Hello
```


</CodeGroup>

Hrm, still not saying `Hello  World`. Pass the `--debug` flag to to see what's going wrong.

<CodeGroup>

```bash {{ title: "Node" }}
$ dotenvx run --debug -- node index.js
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

```bash {{ title: "Python" }}
$ dotenvx run --debug -- flask --app index run
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

```bash {{ title: "PHP" }}
$ dotenvx run --debug -- php index.php
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

```bash {{ title: "Ruby" }}
$ dotenvx run --debug -- ruby index.rb
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

```bash {{ title: "Go" }}
$ dotenvx run --debug -- go run main.go
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

```bash {{ title: "Rust" }}
$ dotenvx run --debug -- cargo run
loading env from .env (.env)
{"JELLO":"World"}
JELLO set
JELLO set to World

# Oops, HELLO not JELLO ^^
```

</CodeGroup>

It's easier to debug with `dotenvx` than [dotenv](https://github.com/motdotla/dotenv).

Fix your `.env` file.

```ini
# .env
HELLO="World"
```

Run it one last time.

<CodeGroup>

```bash {{ title: "Node" }}
$ dotenvx run -- node index.js
[dotenvx@0.38.0] injecting env (1) from .env
Server running on port:3000
# http://localhost:3000
# Hello World
```

```bash {{ title: "Python" }}
$ dotenvx run -- flask --app index run
[dotenvx@0.38.0] injecting env (1) from .env
Running on http://127.0.0.1:5000
# http://127.0.0.1:5000
# Hello World
```

```bash {{ title: "PHP" }}
$ dotenvx run -- php index.php
[dotenvx@0.38.0] injecting env (1) from .env
# Hello World
```

```bash {{ title: "Ruby" }}
$ dotenvx run -- ruby index.rb
[dotenvx@0.38.0] injecting env (1) from .env
Listening on http://127.0.0.1:4567
# http://127.0.0.1:4567
# Hello World
```

```bash {{ title: "Go" }}
$ dotenvx run -- go run main.go
[dotenvx@0.38.0] injecting env (1) from .env
http server started on [::]:1323
# http://localhost:1323
# Hello World
```

```bash {{ title: "Rust" }}
$ dotenvx run -- cargo run
[dotenvx@0.38.0] injecting env (1) from .env
Rocket has launched from http://127.0.0.1:8000
# http://127.0.0.1:8000
# Hello World
```

</CodeGroup>

![](/assets/docs/quickstart/hello-world.png)

🎉 It worked!

## Add Encryption

Add encryption to your `.env` files with a single command. Pass the `--encrypt` flag.

```bash {{ title: "dotenvx set --encrypt" }}
$ dotenvx set HELLO World --encrypt
set HELLO with encryption (.env)
```

Look at your `.env` file.

```ini {{ title: ".env" }}
#/-------------------[DOTENV_PUBLIC_KEY]--------------------/
#/            public-key encryption for .env files          /
#/       [how it works](https://dotenvx.com/encryption)     /
#/----------------------------------------------------------/
DOTENV_PUBLIC_KEY="03c13bb105c307db64cc4b433987f12694bf79231487f44459f63fe70905494913"

# .env
HELLO="encrypted:BGmkvtg+DtseC3xyeJ4ITAqxWWFsFEBxTABZNJjPoMrVVJjkeevmUK719kxqKdlNrsClsvHK2Qs1yfHXNJM6Q6d8QJ/MIRoC0GLlJQkvHVJbf/931FoAVYNit6ahNyCgOt3Y3y6s"
```

It's encrypted! Run your code again with `dotenvx run --`.

<CodeGroup>

```bash {{ title: "Node" }}
$ dotenvx run -- node index.js
[dotenvx@0.38.0] injecting env (2) from .env
Server running on port:3000
# http://localhost:3000
# Hello World
```

```bash {{ title: "Python" }}
$ dotenvx run -- flask --app index run
[dotenvx@0.38.0] injecting env (2) from .env
Running on http://127.0.0.1:5000
# http://127.0.0.1:5000
# Hello World
```

```bash {{ title: "PHP" }}
$ dotenvx run -- php index.php
[dotenvx@0.38.0] injecting env (2) from .env
# Hello World
```

```bash {{ title: "Ruby" }}
$ dotenvx run -- ruby index.rb
[dotenvx@0.38.0] injecting env (2) from .env
Listening on http://127.0.0.1:4567
# http://127.0.0.1:4567
# Hello World
```

```bash {{ title: "Go" }}
$ dotenvx run -- go run main.go
[dotenvx@0.38.0] injecting env (2) from .env
http server started on [::]:1323
# http://localhost:1323
# Hello World
```

```bash {{ title: "Rust" }}
$ dotenvx run -- cargo run
[dotenvx@0.38.0] injecting env (2) from .env
Rocket has launched from http://127.0.0.1:8000
# http://127.0.0.1:8000
# Hello World
```

</CodeGroup>

![](/assets/docs/quickstart/hello-world.png)

🎉 It worked, again! But how?

Look at your `.env.keys` file. It contains the private decryption key.

```ini {{ title: ".env.keys" }}
#/------------------!DOTENV_PRIVATE_KEYS!-------------------/
#/ private decryption keys. DO NOT commit to source control /
#/     [how it works](https://dotenvx.com/encryption)       /
#/----------------------------------------------------------/

# .env
DOTENV_PRIVATE_KEY="ece16b6649d3f3e3a60eb97384484902d65afc85083f3bb5162b0a35b78171fa"
```

The `run` command uses it to decrypt any encrypted values and inject them at runtime. Under the hood it uses the same public-key cryptography as [Bitcoin](https://en.bitcoin.it/wiki/Secp256k1).

This is a huge security improvement for `.env` files.

