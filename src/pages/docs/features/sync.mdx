import { ScreenshotBrowser } from '@/components/ScreenshotBrowser'

export const description = 'Sync your secrets across teams, machines, and environments.'

##### [Features](/docs/features)
# Sync .env files

Sync your secrets securely across your team. {{ className: 'lead' }}

![](/assets/img/features/sync.gif)

* Push and pull changes to your `.env` file(s)
* Share securely between teammates
* Optionally make changes directly in the UI
